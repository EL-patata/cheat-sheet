import { FC } from 'react';

type Props = {};

const PDFview: FC<Props> = ({}) => {
	return (
		<section className="bg-white">
			<header className="h-14 w-full border-b border-border px-2 flex items-center justify-center"></header>
		</section>
	);
};

export default PDFview;
